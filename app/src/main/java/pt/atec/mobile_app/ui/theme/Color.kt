package pt.atec.mobile_app.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta de Cores Principal - AMIGO
val AmigoPurple = Color(0xFF3F3D89)     // Roxo principal da marca
val AmigoPurpleDark = Color(0xFF2D2B66) // Roxo escuro para estados focados
val AmigoPurpleLight = Color(0xFFE8E7FF) // Roxo muito claro para fundos de itens

val AmigoBlue = Color(0xFF0077B6)       // Azul principal da marca
val AmigoBlueDark = Color(0xFF005A8A)   // Azul escuro para contraste
val AmigoBlueLight = Color(0xFFE0F2F1)  // Azul muito claro para realces

// Cores de Interface (Clean Design)
val AmigoBackground = Color(0xFFF8F9FA) // Cinza extremamente claro para o fundo global
val AmigoSurface = Color(0xFFFFFFFF)    // Branco puro para cartões e áreas de conteúdo
val AmigoTextPrimary = Color(0xFF212121) // Preto suave para texto principal
val AmigoTextSecondary = Color(0xFF757575) // Cinza para texto de suporte

// Cores de Estado
val AmigoError = Color(0xFFD32F2F)
val AmigoSuccess = Color(0xFF388E3C)
